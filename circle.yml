machine:
  node:
    version: 6.10.3
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn
  post:
    - yarn build
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test

general:
  artifacts:
    - "coverage"
